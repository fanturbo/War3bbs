package com.umeng.commm.ui.api;


import java.util.concurrent.TimeUnit;

//import okhttp3.OkHttpClient;
//import retrofit2.GsonConverterFactory;
//import retrofit2.Retrofit;

/**
 * Created by turbo on 2015/12/24.
 */
public class ApiClient {
   /* public static final String API_URL = "";
    public static final String DEBUG_API_URL = "";
    private static OkHttpClient client;
    private static ApiClient mApiInterface;

    public static ApiClient getMeirixueApiClient() {

            Retrofit retrofit = null;
    if (mApiInterface == null) {
        client = new OkHttpClient.Builder()
                .connectTimeout(10000, TimeUnit.SECONDS)
                .build();

        retrofit = new Retrofit.Builder()
                .baseUrl(API_URL)
                .client(client)
                .addConverterFactory(GsonConverterFactory.create())
                .build();
    }
    mApiInterface = retrofit.create(ApiClient.class);
    return mApiInterface;
}*/

}
